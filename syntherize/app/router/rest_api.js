import paths from "./paths";
import AccessScope from "../views/RestAPI/AccessScope.vue";

const rest_api_paths = [
    {
        path: '/apps/syntherize/restapi/access_scope',
        name: 'AccessScope',
        component: AccessScope
    }
]

export default rest_api_paths